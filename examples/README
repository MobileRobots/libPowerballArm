
To build all programs, run "make".

To build a specific program, run "make <program>", e.g.:
   make move_ddevice

To run a program, add ../lib to LD_LIBRARY_PATH then run the program.

Use the "move_device" program to test sending commands to a joint.
